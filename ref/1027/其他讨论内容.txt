1.任务类型 VC, TTS
2端到端还是多阶段  --端到端
3.通用性 --one-class
4.各种检测方法 
5.场景  -- 接打电话/ --模型概率判断+场景验证机制设计

应用场景：1)真假同时场景
60s语音中，部分关键信息是插入的 => 15 * 4s,  4s真假

验证机制：
检测到关键信息时候让对方重新说一遍，如果检测到音频信息完全相同(没有细微差别)，
可以视为某种replay(重放或者相同的TTS数据)，判定为异常

One-class训练场景
训练只有True样本

对抗训练场景
True-Negative数据集比例
Negative的少量样本--对抗样本 1%
True 99% --- 增强模型对于异常样本的敏感性

评价：确实也可以讲一段
Accuracy, Recall, Precision. F1


三个transformer-based模型，之后看看